{{#partial "content"}}

    <div class="row">
        <div class="col-md-6">
            <h1>Enter Your Hero Details</h1>
            <form action='/heroes' method="POST">
                {{#if squads}}
                <div class="form-group">
                    <label for="name">Hero Name:</label>
                    <input id="name" name="name" class="form-control" type="text">
                </div>
                <div class="form-group">
                    <label for="nickname">Hero Code Name:</label>
                    <input id="nickname" name="nickname" class="form-control" type="text">
                </div>
                <div class="form-group">
                    <label for="age">Hero Age:</label>
                    <input id="age" name="age" class="form-control" type="number">
                </div>
                <div class="form-group">
                    <label for="power">Powers:</label>
                    <input id="power" name="power" class="form-control" type="text">
                </div>
                <div class="form-group">
                    <label for="weakness">Weakness:</label>
                    <input id="weakness" name="weakness" class="form-control" type="text">
                </div>
                <div class="form-group">
                    <label for="squadId">Enter Hero's Squad</label>
                    <select id="squadId" name="squadId" class="form-control">
                        {{#each squads}}
                            <option value="{{squad.id}}">{{squad.name}}</option>
                        {{/each}}
                    </select>
                </div>
                    <p>Squad Not Found?<a href="/squads/new">Add New Squad</a></p>
                {{else}}
                    <h3>Add Squad First before Adding a Hero. <a href="/squads/new">Add Squad Here</a></h3>
                {{/if}}
                 <button type="submit" class="btn">Add Hero</button>
            </form>
            <!--form.hbs.-->
        </div>
    </div>
{{/partial}}

{{> layout.hbs}}